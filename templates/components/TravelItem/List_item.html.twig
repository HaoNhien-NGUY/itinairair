<div id="item_{{ item.id }}">
    <div class="border-1 border-gray-200 bg-white rounded-2xl p-2.5 {{ newItem is same as(item) ? 'new-item' }}">
        <div class="flex space-x-5">
            <div>
                <div class="relative">
                    <img class="size-20 rounded-xl shrink-0 object-cover" src="{{ item.place.photoUri ?? ('https://placehold.co/300?text=' ~ item.name) }}" alt="{{ item.name }}-photo" />
                    <div class="absolute  inline-flex items-center justify-center w-6 h-6 text-xs font-bold text-white bg-black bg-danger border-2 border-buffer rounded-full -bottom-1 -end-1">
                        {{ position }}
                    </div>
                </div>
            </div>
            <div class="">
{#                <p class="text-sm text-purple-900 font-medium -mt-1.5">{{ item.place.type }}</p>#}
                <div class="font-medium text-base/6 line-clamp-2">{{ item.place.name }}</div>
                <a href="{{ item.place.googleMapsURI }}" target="_blank" rel="noopener noreferrer" class="text-sm/snug -mt-0.5 hover:text-gray-700 text-gray-500 line-clamp-1">{{ item.place.address }}</a>
                {% if item.notes is not empty %}
                    <div class="mt-3 border-1 border-gray-300 rounded-xl text-sm border-dashed px-3 py-1">
                        <span class="text-gray-600">
                            {{ item.notes }}
                        </span>
                    </div>
                {% endif %}
            </div>

            <div class="self-center justify-self-end">
                <form action="{{ path('app_travelitem_delete', {'item': item.id}) }}" method="post">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item.id) }}">
                    <button type="submit" class="cursor-pointer text-red-600 hover:text-red-400 underline transition-colors">Delete</button>
                </form>
            </div>
        </div>
    </div>
</div>
