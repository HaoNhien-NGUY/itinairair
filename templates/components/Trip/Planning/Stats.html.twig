<div {{ attributes.defaults({class: 'p-2 text-sm bg-white border-gray-200 border rounded-2xl' })}}>
    <a href="{{ path('app_trip_bookings', {'id': trip.id}) }}"
       class=" hover:bg-indigo-50 transition-colors rounded-xl font-medium flex items-center justify-between text-gray-500 py-2 px-3">
        <span class="flex items-center">{{ 'common.hotels_and_accommodations'|trans }}
            <twig:ux:icon name="bi:dash-lg" class="mx-2.5 size-4"/>
            {{- this.accommodationCount -}}
        </span>
        <twig:ux:icon name="iconoir:nav-arrow-right" class="size-5"/>
    </a>
    <hr class="border border-gray-100 my-1.5 mx-6"/>
    <a href="{{ path('app_trip_bookings', {'id': trip.id}) }}"
       class=" hover:bg-indigo-50 transition-colors rounded-xl font-medium flex items-center justify-between text-gray-500 py-2 px-3">
        <span class="flex items-center">{{ 'common.flights'|trans }}
            <twig:ux:icon name="bi:dash-lg" class="mx-2.5 size-4"/>
            {{- this.flightCount -}}
        </span>
        <twig:ux:icon name="iconoir:nav-arrow-right" class="size-5"/>
    </a>
</div>
