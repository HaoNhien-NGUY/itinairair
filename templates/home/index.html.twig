{% extends 'base_with_navbar.html.twig' %}

{% block title %}{{ app_name }}{% endblock %}

{% block body %}
    <section class="relative pt-12 pb-20 lg:pt-24 lg:pb-32 overflow-hidden" data-turbo-prefetch="false">
        <div class="px-4 mx-auto max-w-7xl sm:px-6 lg:px-8 text-center">
            <h1 class="text-5xl font-black tracking-tight text-gray-900 sm:text-7xl mb-6">
                {{ 'landing.title_1'|trans }}  <span class="text-transparent bg-clip-text bg-gradient-to-r from-indigo-600 to-blue-500">{{ 'landing.title_2'|trans|lower }}</span> {{ 'landing.title_3'|trans|lower }}.
{#                <br>#}
{#                <span class="text-transparent bg-clip-text bg-gradient-to-r from-indigo-600 to-blue-500">{{ 'landing.title_2'|trans|lower }}.</span>#}
            </h1>
            <p class="mt-6 text-lg text-gray-500 max-w-2xl mx-auto mb-10">
                {{ 'landing.subtitle'|trans }}
            </p>

            <div class="flex flex-col sm:flex-row justify-center items-center gap-5 md:gap-3 mb-16">
                <a href="{{ path('app_trip') }}" class="px-8 py-3.5 btn-animate text-base text-white font-semibold bg-indigo-500 rounded-full hover:bg-indigo-400 transition shadow-xl shadow-indigo-500/20">
                    {{ 'landing.cta'|trans }}
                </a>
                {% if not app.user %}
                    <form action="{{ path('app_demo_start') }}" method="post" class="">
                        <input type="hidden" name="_token" value="{{ csrf_token('start_demo') }}">
                        <button type="submit" class="cursor-pointer px-6 py-2 btn-animate font-semibold text-base text-indigo-500 rounded-full inline-flex flex-col justify-center items-center hover:bg-indigo-50 transition">
                            <span class="flex items-center justify-center gap-2">
                                {{ 'landing.cta_demo'|trans }}
                                <twig:ux:icon name="maki:arrow" class="size-4" />
                            </span>
                            <span class="text-xs text-slate-500 mt-1 font-normal">
                                {{ 'landing.cta_demo_subtitle'|trans }}
                            </span>
                        </button>
                    </form>
                {% endif %}
            </div>


            <div class="relative max-w-5xl mx-auto rounded-2xl shadow-2xl border border-gray-200 overflow-hidden bg-gray-50">
                <div class="bg-white p-2 border-b border-gray-200 flex gap-2">
                    <div class="w-3 h-3 rounded-full bg-red-400"></div>
                    <div class="w-3 h-3 rounded-full bg-yellow-400"></div>
                    <div class="w-3 h-3 rounded-full bg-green-400"></div>
                </div>
                <div class="bg-indigo-10 flex items-center justify-center text-gray-300">
                    <video class="w-full" autoplay loop muted playsinline poster="{{ asset('images/hero-banner.png') }}">
                        <source src="{{ asset('images/hero-banner.webm') }}" type="video/webm">
                        Your browser does not support the video tag.
                    </video>
                </div>
            </div>
        </div>
    </section>



    <section class="py-12 max-w-7xl mx-auto bg-white relative">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div class="zbg-yellow-50 p-4 rounded-3xl border-2z border-yellow-200">
                    <div class="text-4xl mb-6">🏨</div>
                    <h3 class="text-xl font-bold text-gray-900 mb-2">{{ 'landing.features.one.title'|trans }}</h3>
                    <p class="text-gray-700">
                        {{ 'landing.features.one.subtitle'|trans }}
                    </p>
                </div>

                <div class="zbg-blue-50 p-4 rounded-3xl border-2z border-blue-200">
                    <div class="text-4xl mb-6">🗓️</div>
                    <h3 class="text-xl font-bold text-gray-900 mb-2">{{ 'landing.features.two.title'|trans }}</h3>
                    <p class="text-gray-700">
                        {{ 'landing.features.two.subtitle'|trans }}
                    </p>
                </div>

                <div class="zbg-pink-50 p-4 rounded-3xl border-2z border-pink-200">
                    <div class="text-4xl mb-6">🌍</div>
                    <h3 class="text-xl font-bold text-gray-900 mb-2">{{ 'landing.features.three.title'|trans }}</h3>
                    <p class="text-gray-700">
                        {{ 'landing.features.three.subtitle'|trans }}
                    </p>
                </div>
            </div>
        </div>
    </section>

    <footer class="bg-gray-50 border-t border-gray-200 py-10 text-center text-sm">
        <p class="text-gray-600 font-medium">
        <p class="mt-2">Développé par <span class="text-indigo-400 font-medium">Hao-Nhien NGUYEN</span>.</p>

        <div class="flex text-slate-400 font-medium justify-center mt-6 gap-6">
            <a href="#" target="_blank" class="hover:text-slate-500 transition">LinkedIn</a>
            <a href="https://github.com/HaoNhien-NGUY/itinairair" target="_blank" class="hover:text-slate-500 transition">GitHub</a>
            <a href="mailto:haonhien.nguy@gmail.com" class="hover:text-slate-500 transition">Me contacter</a>
        </div>
    </footer>
{% endblock %}
